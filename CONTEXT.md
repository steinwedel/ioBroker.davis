# CONTEXT

**Current Task**: Remove redundant `@types/mocha` (E0063).

**Key Decisions**:
- Mocha types stay available via `@iobroker/testing`; tsconfig still lists `"mocha"`.

**Next Steps**:
- Publish 0.0.18 when asked; haus20a is still on 0.0.16.
